import { AppTheme } from "@/src/shared/styles/general.styles";
import { StyleSheet } from "react-native";

export const inviteCreateFormStyles = (theme: AppTheme) => {
  return StyleSheet.create({});
};
